{
  "name": "@esp32-agent-companion/daemon",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test dist/test/*.test.js",
    "start": "node dist/src/cli.js daemon",
    "status": "node dist/src/cli.js status",
    "install:openclaw": "npm run build && node dist/src/cli.js install-character openclaw ../assets/openclaw-lab.bin",
    "install:jarvis": "npm run build && node dist/src/cli.js install-character jarvis ../assets/jarvis-lab.bin",
    "install:daemon": "npm run build && node dist/src/install.js",
    "install:macos": "npm run install:daemon"
  },
  "dependencies": {
    "serialport": "13.0.0"
  },
  "devDependencies": {
    "@types/node": "24.10.1",
    "typescript": "7.0.2"
  }
}
